# Get a free key at https://www.deepl.com/pro-api (free tier: 500,000 chars/month).
# Free-tier keys end in ":fx" — the dev server auto-detects this and routes to
# the free API endpoint instead of the paid one.
DEEPL_API_KEY=
